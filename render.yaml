services:
  - type: web
    name: agent-door
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: PORT
        value: "10000"
    plan: free
    autoDeploy: true
